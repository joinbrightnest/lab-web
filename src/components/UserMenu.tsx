"use client";

/** Re-export — use UserMenuDropdown. */
export {
  UserMenuDropdown as UserMenu,
  UserMenuDropdown,
  resolveAvatarSrc,
  type UserMenuDropdownProps,
  type UserMenuDropdownProps as UserMenuProps,
} from "@/components/UserMenuDropdown";
